/** Quill SizeStyle whitelist: 8px–36px (inclusive). */
export const GMAIL_QUILL_SIZE_PX_OPTIONS: string[] = Array.from({ length: 29 }, (_, i) => `${i + 8}px`);

export const GMAIL_DEFAULT_SIZE_PX = "11px";
