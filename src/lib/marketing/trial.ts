/** Product marketing: length of the free trial (billing enforcement may live elsewhere). */
export const FREE_TRIAL_DAYS = 14;

export const freeTrialPhrase = `${FREE_TRIAL_DAYS}-day free trial`;

export const freeTrialCtaLanding = `Start ${FREE_TRIAL_DAYS}-day free trial`;

export const freeTrialCtaPricing = `Start ${FREE_TRIAL_DAYS}-day trial`;

/** Sentence fragment for meta descriptions (starts with a word, not a digit). */
export const freeTrialMetaSnippet = `Includes a ${FREE_TRIAL_DAYS}-day free trial`;
